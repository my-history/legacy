$(document).ready(function () {

	let home = 'Главная'

	let famous = 'Известные личности'
	let dates = 'Важные даты'
	let streets = 'Улицы края'
	let ww2 = 'Великая война'

	let test = 'Тест'

	let about = 'О проекте'
	let thanks = 'Благодарности'
	let sources = 'Источники'

	$('.navigation__item.home').text(home)
	$('.navigation__item.famous').text(famous)
	$('.navigation__item.dates').text(dates)
	$('.navigation__item.streets').text(streets)
	$('.navigation__item.ww2').text(ww2)
	$('.navigation__item.test').text(test)
	$('.navigation__item.about').text(about)
	$('.navigation__item.thanks').text(thanks)
	$('.navigation__item.sources').text(sources)

})
